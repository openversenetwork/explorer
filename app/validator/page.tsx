import SupplyPageClient from './page-client';

export const metadata = {
    description: `validator`,
    title: `Validator | Openverse`,
};

export default function SupplyPage() {
    return <SupplyPageClient />;
}
